#pragma once

// Make More 


void InitHooks()
{

	//if Using Minhook use This as a template 


	/*
	
	MH_CreateHook(reinterpret_cast<void**>(ModuleGA + (0x0000)), &your_FunctionName_Hook, (void**)&your_FunctionName_Original);
	MH_EnableHook(reinterpret_cast<void**>(ModuleGA + (0x0000)));
	
	*/
}